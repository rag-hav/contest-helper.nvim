local window = require("contest-helper.window")


local M = {}
M.displayResults = function(testCaseInputs, testCaseAnswers, testCaseOutputs, testCaseTimeTaken, testCaseErrors)
    local bufnr = window.getResultsBuffer()
end

return M
